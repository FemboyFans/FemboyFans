<% content_for(:html_header) do %>
  <meta name="deferred-posts" content="<%= deferred_posts.keys.join(",") %>">
  <% deferred_posts.each do |id, post| %>
    <%= tag.meta(name: "deferred-posts-#{id}", content: post.to_json) %>
  <% end %>
<% end %>
